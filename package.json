{
  "name": "crayon-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "overrides": {
      "@tigerdata/mcp-boilerplate": "file:packages/core/docker/tigerdata-mcp-boilerplate-1.1.0.tgz"
    }
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
