# 0pflow Auth Server Environment Variables

# Nango secret key (0pflow's shared Nango account)
NANGO_SECRET_KEY=

# Tiger Cloud PostgreSQL connection string (auth-server's own metadata DB)
DATABASE_URL=

# Shared TimescaleDB for provisioning workspace schemas (optional).
# When set, users can create cloud workspaces without a personal Tiger database.
# Must be an admin connection string (tsdbadmin or equivalent).
DATABASE_DATA_URL=

# GitHub OAuth app credentials
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Public GitHub client ID (for browser-side OAuth redirect)
NEXT_PUBLIC_GITHUB_CLIENT_ID=

# Fly.io API token (for managing user app deployments)
FLY_API_TOKEN=

# Fly.io organization (default: personal)
FLY_ORG=tiger-data

# Ed25519 private key for signing dev-ui JWTs (PEM format, newlines as \n)
# Generate with: node -e "const c=require('crypto');const k=c.generateKeyPairSync('ed25519');console.log(k.privateKey.export({type:'pkcs8',format:'pem'}))"
DEV_UI_JWT_PRIVATE_KEY=
